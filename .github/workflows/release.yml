name: release

on:
  workflow_dispatch:
    inputs:
      expected_version:
        description: Expected .xgc2/product.yml version
        required: true
        type: string
      expected_source_sha:
        description: Expected source commit SHA
        required: true
        type: string
      publish_apt:
        description: Publish debs to the XGC2 APT repository
        required: true
        type: boolean
        default: false
      run_cpp_quality:
        description: Run optional C++ quality checks
        required: true
        type: boolean
        default: false
      run_source_tests:
        description: Run optional source tests
        required: true
        type: boolean
        default: false

jobs:
  release-guard:
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.ref.outputs.branch }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Select package branch
        id: ref
        run: |
          set -euo pipefail
          branch="${GITHUB_REF_NAME}"
          case "${branch}" in
            v1.12-noetic|v1.14-noetic|v1.16-jazzy) ;;
            *)
              echo "::error::release.yml must be dispatched with a package branch ref, got ${branch}"
              exit 1
              ;;
          esac
          echo "branch=${branch}" >> "${GITHUB_OUTPUT}"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ref.outputs.branch }}

      - name: Validate release inputs
        run: |
          set -euo pipefail
          product_version="$(awk -F': *' '/^version:[[:space:]]*/ {print $2; exit}' .xgc2/product.yml)"
          if [[ "${product_version}" != "${{ inputs.expected_version }}" ]]; then
            echo "product.yml version ${product_version} != expected_version ${{ inputs.expected_version }}" >&2
            exit 1
          fi
          source_sha="$(git rev-parse HEAD)"
          if [[ "${source_sha}" != "${{ inputs.expected_source_sha }}" ]]; then
            echo "HEAD ${source_sha} != expected_source_sha ${{ inputs.expected_source_sha }}" >&2
            exit 1
          fi

  build:
    needs: release-guard
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    env:
      APT_REPO_HOST: ${{ secrets.APT_REPO_HOST }}
      APT_REPO_PORT: ${{ secrets.APT_REPO_PORT }}
      APT_REPO_SSH_KEY: ${{ secrets.APT_REPO_SSH_KEY }}
      APT_REPO_KNOWN_HOSTS: ${{ secrets.APT_REPO_KNOWN_HOSTS }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-guard.outputs.branch }}

      - name: Build and install-check runtime packages in Docker
        run: |
          .xgc2/scripts/build_runtime_deb_in_docker.sh \
            --work-dir "$RUNNER_TEMP/px4-runtime-work-${{ needs.release-guard.outputs.branch }}-${{ matrix.arch }}" \
            --output-dir "$GITHUB_WORKSPACE/debs"

      - name: Upload deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: px4-sitl-runtime-debs-${{ needs.release-guard.outputs.branch }}-${{ matrix.arch }}
          path: debs/*.deb

      - name: Publish to self-hosted APT repository
        if: ${{ inputs.publish_apt && env.APT_REPO_HOST != '' && env.APT_REPO_SSH_KEY != '' && env.APT_REPO_KNOWN_HOSTS != '' }}
        run: .xgc2/scripts/publish_apt_repo.sh --deb-dir debs
